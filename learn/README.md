# RL Handbook

挂在 [carsontung666/RL-Kernel](https://github.com/carsontung666/RL-Kernel) 的 **`gh-pages`** 分支上，不碰 fork 的 `main`。

**https://carsontung666.github.io/RL-Kernel/learn/**

入门总览，加上四块知识：基础、深度 RL、LLM 对齐、系统层；资料页只列仓库和课程。

本 fork 的 `gh-pages` 根目录仍是 MkDocs 项目文档；这本手册在 `/learn/`。
